<% content_for(:title) { "#{@namespace} / #{@page}" } %>

<nav class='actions'>
  <%= link_to '🔝', :root %>
  <%= link_to '🔙', @namespace %>
  <br>
  <%= link_to '🔷', edit_namespace_page_path(@namespace, @page), class: 'js-page-edit' %>
</nav>

<div class='page'>
  <%= @page.to_html.html_safe %>
</div>
