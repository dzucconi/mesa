<% content_for(:title) { "Editing #{@namespace} / #{@page}" } %>

<div class='actions'>
  <%= link_to @page, namespace_page_path(@namespace, @page) %>
  <span>|</span>
  <%= link_to 'Χ', [@namespace, @page], method: :delete, data: { confirm: 'Are you sure?' }, class: 'is-dangerous' %>
</div>

<%= render 'form' %>
