<% content_for(:title) { "Editing #{@namespace} / #{@page}" } %>

<% content_for :breadcrumb do %>
  <%= link_to 'Root', :root %>
  <%= link_to @namespace, @namespace %>
  <%= link_to @page, namespace_page_path(@namespace, @page) %>
  <%= content_tag :span, 'Settings' %>
<% end %>

<%= render 'form' %>
